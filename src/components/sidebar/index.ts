/**
 * Sidebar Components Registry
 * 
 * This file serves as a registry for all sidebar preview components.
 * It maps component names to their implementations.
 */

import TextNodePreview from './TextNodePreview';

/**
 * Registry of all sidebar preview components
 */
const previewComponents = {
  TextNodePreview,
  // Add more preview components here as they are created
};

export default previewComponents;
